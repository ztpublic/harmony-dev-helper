# project-detector-rs

Internalized Rust fork of the upstream project detector source from `external-src/project-detector-main`.

This crate is intentionally Rust-native only. It removes the upstream N-API packaging and is not wired into runtime code in this repository yet.
